---
layout: base.njk
---
<div class="page-head{% if narrow %} page-head--narrow{% endif %}">
    <h1 class="page-title">{{ title }}</h1>
    {% if subtitle %}<p class="page-subtitle">{{ subtitle }}</p>{% endif %}
</div>

{{ content | safe }}
